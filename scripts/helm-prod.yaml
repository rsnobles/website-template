# Overrides for prod environment

# NOTE: Enable build webhook for prod environment by uncommenting these lines
# full-stack:
#   persistence:
#     enabled: true
#
#   services:
#     www:
#       # webhook and www must be placed on the same node
#       # because of ReadWriteOnce persistent disk access mode
#       placementSameAs: webhook
#       replicas: 1 # TODO: remove?
#       env:
#         REPLACEMENT_DISABLED: true
#     webhook:
#       enabled: true
#       placementPreferSameAs: www
