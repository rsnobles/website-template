# Overrides for dev environment

# NOTE: Enable build webhook for dev environment by uncommenting these lines
# full-stack:
#   services:
#     www:
#       replicas: 1
#       sidecar:
#         enabled: true
#         env:
#           VC_PULL_ENABLED: false
